#include <stdio.h>
int main() {
    char word[50];
    int i=1, lim;
    printf("Enter the Word: ");
    scanf("%s", &word);
    
    printf("Enter line number: ");
    scanf("%d", &lim);
    
  while(i<=lim){
      printf("%s\n", word);
      i++;
  }
  return 0;
    
}
